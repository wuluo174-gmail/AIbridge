"""
Bridge CLI Adapters
==================
CLI 工具适配器注册表。

Step 3 时实现 AdapterRegistry。
Step 7 时实现自动发现和角色配置。
"""

# Step 7 时实现:
# class AdapterRegistry:
#     def discover(self) -> list[CLIAdapter]: ...
#     def register(self, adapter: CLIAdapter): ...
#     def get_by_id(self, tool_id: str) -> CLIAdapter | None: ...
